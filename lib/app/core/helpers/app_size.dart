import 'package:get/get.dart';

class AppSize {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

  static double sTen = screenWidth * 0.03;

  // * font sizes
  static double bigText = 30;
  static double mediumText = 20;
  static double smallText = 15;
  static double extraSmallText = 12;

  static double fTen = screenWidth * 0.03;
  static double fTwelve = screenWidth * 0.035;
  static double fFourteen = screenWidth * 0.04;
  static double fSixteen = screenWidth * 0.045;
  static double fEighteen = screenWidth * 0.05;
  static double fTwenty = screenWidth * 0.055;
  static double fTwentyTwo = screenWidth * 0.06;
  static double fTwentyFour = screenWidth * 0.065;
  static double fTwentySix = screenWidth * 0.07;
  static double fTwentyEight = screenWidth * 0.075;
  static double fThirty = screenWidth * 0.08;

  // * icon sizes
  static double iTen = screenWidth * 0.03;
  static double iTwelve = screenWidth * 0.035;
  static double iFourteen = screenWidth * 0.04;
  static double iSixteen = screenWidth * 0.045;
  static double iEighteen = screenWidth * 0.05;
  static double iTwenty = screenWidth * 0.055;
  static double iTwentyTwo = screenWidth * 0.06;
  static double iTwentyFour = screenWidth * 0.065;

  // * home screen

  // * Header height
  static double headerHeight = screenHeight * 0.3;
  static double iSliderHeight = screenHeight * 0.225;
  static double headerContainerTopGap = screenHeight * 0.15;
  static double headerContainerSideGap = screenWidth * 0.05;
  static double headerInfoHeight = AppSize.screenHeight * 0.15;

  // Radius for the container
  static double rTen = screenWidth * 0.03;
  static double rTwenty = screenWidth * 0.055;

  // * padding sizes
  static double pFive = 5;
  static double pTen = 10;
  static double pTwelve = 12;
  static double pFifteen = 15;
  static double pTwenty = 20;

  // * image sizes
  static double imTwenty = screenWidth * 0.055;
  static double imTwentyTwo = screenWidth * 0.06;
  static double imTwentyFour = screenWidth * 0.065;
  static double imTwentySix = screenWidth * 0.07;
  static double imTwentyEight = screenWidth * 0.075;
  static double imThirty = screenWidth * 0.08;
  static double imThirtyTwo = screenWidth * 0.085;
  static double imThirtyFour = screenWidth * 0.09;
  static double imThirtyFive = 30.0;
  static double imThirtySix = screenWidth * 0.095;
  static double imThirtyEight = screenWidth * 0.1;
  static double imForty = screenWidth * 0.105;

  // * height size
  static double hFive = 5;
  static double hTen = 10;
  static double hFifteen = 15;
  static double hTwenty = 20;

  // * width size
  static double wFive = 5;
  static double wTen = 10;
  static double wFifteen = 15;
  static double wTwenty = 20;
  static double wTwentyFive = 25;
  static double wThirty = 30;
  static double wThirtyFive = 35;
  static double wForty = 40;

  // Shariah Investment sizes
  static double siConHeight = screenHeight * 0.09;
  static double siConWidth = AppSize.screenWidth * 0.25;

  // Best Offer sizes
  static double listHeight = screenHeight * 0.35;
  static double listConWidth = screenWidth * 0.43;
  static double listConImHeight = screenHeight * 0.13;

  // Healthy Product sizes
  static double hPConHeight = screenHeight * 0.3;

  // Button sizes
  static double bHeight = screenHeight * 0.05;
}
