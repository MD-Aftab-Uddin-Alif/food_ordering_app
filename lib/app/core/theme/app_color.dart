import 'package:flutter/material.dart';

class AppColor {
  // Card color
  static const primary = Colors.white;
  static const secondary = Colors.green;

  // * bg color

  static final shadow = Colors.grey.withOpacity(0.3);

  // * text color
  static const bText = Colors.black;
  static const wText = Colors.white;
  static final bTextLight = Colors.black.withOpacity(0.7);

  // * button color

  // * icon color
  static const wIcon = Colors.white;
  static const bIcon = Colors.black;
  static const gIcon = Colors.green;
}
